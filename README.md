CIFAKE robustness comparison of a baseline CNN and ResNet-18 across seeds 42–44.
`Group_Neural_Networks_Cifake.ipynb` is the analysis notebook; `resnet18_runner_COLAB.ipynb` is the Colab training runner.
`results/` holds all run bundles that the notebooks validate against.
